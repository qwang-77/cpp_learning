#include<iostream>
#include<string>
using namespace std;

int main() {
	//1. create a bool type cariable
	bool flag = true;
	cout << flag << endl; // 1 for true, 0 for false
	cout << sizeof(bool) << endl; // take only 1 bit
	system("pause");
	return 0;
}

